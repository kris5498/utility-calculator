spring:
  application:
    name: utility-calculator
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: true

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
  endpoint:
    health:
      show-details: never

